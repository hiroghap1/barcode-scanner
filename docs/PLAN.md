# 開発計画書

docs/PRD.md・docs/DESIGN.md に基づく MVP 開発の進行計画。

## 方針

1. **最大リスクを最初に潰す** — このアプリの成否はスキャン体験で決まる。VisionKit はシミュレータで動かず実機必須のため、最初に実機で技術検証（スパイク）を行う
2. **シミュレータで進められる部分を厚くする** — 取込・一覧・出力はシミュレータ＋単体テストで高速に開発できるので、スキャン検証と並行して進める
3. **各フェーズの終わりに「動くもの」がある** — フェーズごとに完了条件（動作確認できること）を定義する

---

## フェーズ一覧

| フェーズ | 内容 | 規模感 |
|---------|------|--------|
| P0 | プロジェクト準備 + スキャン技術スパイク | S |
| P1 | データモデル + 取込（S2, S3） | M |
| P2 | レコード一覧 + ホーム(S4, S1) | M |
| P3 | スキャン本実装（S5） | L |
| P4 | 出力（S6） | S |
| P5 | 仕上げ（非機能要件・エッジケース） | M |
| P6 | MVP 受入テスト | S |

規模感の目安: S = 半日〜1日 / M = 1〜2日 / L = 2〜4日（集中作業時）

---

## P0: プロジェクト準備 + スキャン技術スパイク

**目的**: 開発基盤を作り、最大の技術リスク（カメラ読取）を最初に検証する。

作業:
- [x] Xcode プロジェクト作成（iOS 17+ / SwiftUI / SwiftData）
- [x] `Info.plist` に `NSCameraUsageDescription` 追加
- [x] DESIGN.md 6章のディレクトリ構成を作成
- [x] **スパイク**: 使い捨て画面で VisionKit `DataScannerViewController` を実機動作させ、以下を確認
  - [x] 対応シンボロジーの読み取り（JAN-13 / QR を両エンジンで確認。残りは API 共通のためリスク低、P3 で追確認）
  - [x] 読取速度・精度は実用に足るか（良好。小さいコード・かすれは P3 のテストで確認）
  - [x] AVFoundation フォールバックの動作確認（実機で読取成功）
- [x] スパイク結果を `docs/notes/scanner-spike.md` に記録（採用構成の判断根拠）

**完了条件**: 実機でバーコードを読み取り、値がコンソールに出る。スキャナ構成の判断が下りている。

> スパイクで重大な問題が出た場合（読取精度不足など）はここで設計を見直す。後工程への影響が最も大きい不確実性のため、先頭に置く。

---

## P1: データモデル + 取込

**目的**: 表データをアプリに取り込めるようにする。シミュレータのみで完結。

作業:
- [x] SwiftData モデル実装（`Project`, `Row`, `RowStatus`）
- [x] `TableParser` 実装（RFC 4180、区切り文字自動判定、UTF-8/Shift_JIS）
- [x] `TableParser` 単体テスト（クォート・改行入りセル、空行、列数不揃い、BOM）
- [x] S2 データ取込画面（ペースト / fileImporter）
- [x] S3 プレビュー & 列マッピング画面（自動推定、新規列追加、バリデーション）
- [x] 取込確定 → `Project` 保存(既存コードがある行は「登録済み」化。`ImportPlanner` として Core に実装)

検証: 取込フローの E2E は `BarcodeAssignUITests`(ペースト取込 + CSV ファイル取込)で自動化済み。

**完了条件**: スプレッドシートからコピーした表と CSV ファイルの両方を取り込み、列マッピングして保存できる。パーサのテストが全て通る。

---

## P2: レコード一覧 + ホーム

**目的**: 取り込んだデータの閲覧・管理と、再開の入口を作る。

作業:
- [x] S4 レコード一覧（状態アイコン、フィルタ、検索、行スワイプ操作）
- [x] S1 ホーム（プロジェクト一覧、進捗表示、削除・名前変更、空状態）
- [x] 画面遷移の結線(S1 → S2 → S3 → S4、S1 → S4)
- [x] 1,000行規模のダミーデータでスクロール性能確認(UI テストで自動化)

備考: S4 の「スキャン開始」結線は P3、「出力」ボタンは P4 で追加する。
再起動→ホームから再開の完了条件は UI テスト(testPasteImportFlowSavesProject)で検証済み。

**完了条件**: 取込 → 一覧確認 → アプリ再起動 → ホームから同じデータを開き直せる（再開要件の土台）。

---

## P3: スキャン本実装

**目的**: アプリの中核である連続スキャンフローを完成させる。実機中心の開発。

作業:
- [x] スキャナ実装（P0 スパイクの VisionKit / AVFoundation ラッパーを Views/Scan へ本実装化。ガイド枠 + ScanArbiter 方式）
- [x] S5 スキャン画面 UI（カメラプレビュー、商品情報カード、進捗、操作ボタン）
- [x] 読取成功フロー: 保存 → 音 → 振動 → 次の未登録行へ自動移動（ScanNavigator として Core に実装・テスト付き）
- [x] 同一コードの誤登録防止（ScanArbiter: 再アーム + グローバルクールダウン 1 秒）
- [x] 重複チェック + 警告ダイアログ（登録済み行の情報表示、このまま登録/キャンセル）
- [x] スキップ / 前へ / 一覧へ / 手動入力
- [x] 登録済み行の再スキャン時の上書き確認
- [x] 全行完了画面（出力への誘導は P4 で結線）
- [x] カメラ権限拒否時の誘導表示
- [x] `currentPosition` の逐次保存（途中再開）
- [x] **実機テスト**(2026-07-27): 連続スキャン・途中終了→再開・重複警告をユーザーが実機確認 OK

検証: 手動入力経由のスキャンフロー(登録→自動送り→重複警告→スキップ→完了)は
UI テスト(testScanFlowWithManualEntry)で自動化済み。カメラ読取は実機でのみ検証可能。

**完了条件**: 実機で「表示 → スキャン → 自動保存 → 次へ」を 50 件連続で快適に実行できる。途中でアプリを終了しても続きから再開できる。重複警告が機能する。

---

## P4: 出力

**目的**: 作業結果を外部へ渡せるようにする。

作業:
- [x] `Exporter` 実装（TableExporter は P0 で実装済み。ExportTableBuilder で書込列への `barcode` 合成・書込列のみを追加）
- [x] `Exporter` 単体テスト（クォート・ヘッダー有無は TableExporterTests、合成・書込列のみは ExportTableBuilderTests）
- [x] S6 出力シート（プレビュー、オプション、コピー / fileExporter / Share Sheet。S4 の出力ボタンと S5 完了画面の「出力へ」を結線）
- [x] 実機 → Google スプレッドシートへの貼り付け確認(2026-07-27 ユーザー確認 OK。CSV 保存も問題なし)

**完了条件**: コピーした結果をスプレッドシートに貼り付けて列が正しく揃う。CSV を Excel / Numbers で開ける（文字化けなし）。

---

## P5: 仕上げ（非機能要件・エッジケース）

**目的**: PRD の非機能要件を満たし、実運用に耐える品質にする。

作業:
- [x] バックグラウンド復帰時のカメラセッション再開（didBecomeActive 監視を両スキャナに実装）
- [ ] Dynamic Type 確認（最大サイズでレイアウト崩れがないこと）→ ユーザーの見た目フィードバック時に確認
- [x] VoiceOver 対応（行・カードのまとめラベル、スキャン成功時の読み上げ通知を実装。実機確認は見た目フィードバック時）
- [ ] ダークモード確認（全画面）→ ユーザーの見た目フィードバック時に確認
- [x] エッジケース: 空データ取込(次へ無効 + 検出不可表示)/ 1列だけの表(推定は単体テスト済み・同一列警告表示)/ 巨大データ(5,000行の取込・スクロールを UI テストで確認)/ 不正データ(文字コード判別不能はエラー表示、列数不揃い・クォートはパーサ対応済み)
- [x] アプリアイコンの設定(ユーザー提供の SVG を design/ に保存し、full-bleed 化して 1024px PNG を生成。AccentColor もアイコンの緑 #18B978(ダーク: #53DFA0)で統一)
- [x] ホームの開発用スパイクセクションを削除(SpikeScanView も削除、履歴は git に残る)

**完了条件**: 非機能要件チェックがすべて通る。

---

## P6: MVP 受入テスト

PRD「MVP完成条件」をそのまま受入基準として消化する。

- [ ] CSV を読み込める
- [ ] コピー＆ペーストで表を取り込める
- [ ] 列マッピングできる
- [ ] バーコードを連続スキャンできる
- [ ] 自動保存できる
- [ ] 重複チェックできる
- [ ] CSV を書き出せる
- [ ] Google スプレッドシートへ貼り付けできる
- [ ] 作業途中から再開できる

加えて実データでの通しテスト:
- [ ] 実際の商品リスト（100件以上）で取込 → 全件スキャン → 出力 → スプレッドシート反映までを通しで実行

**完了条件**: 全項目クリア。この時点で MVP 完成。

---

## リスクと対策

| リスク | 影響 | 対策 |
|--------|------|------|
| VisionKit の読取精度・対応端末の制約 | 中核機能が成立しない | P0 で最初にスパイク検証。AVFoundation フォールバックを用意 |
| CSV の方言（文字コード・クォート） | 取込失敗 | パーサを UI 非依存にして単体テストを厚くする（P1） |
| 大量行でのパフォーマンス | 一覧・保存が重い | P2 で 1,000 行、P5 で 5,000 行の負荷確認 |
| 連続スキャンの誤登録（同一コード連続検出） | データ破損・信頼低下 | クールダウン実装 + P3 完了条件で 50 件連続テスト |

## スコープ外（将来拡張 — PRD 参照）

Google Sheets API / Excel / Shopify / Amazon / WooCommerce / Bluetooth リーダー / OCR / NFC / iCloud 同期。
`BarcodeScannerService` プロトコルと `TableParser` / `Exporter` の分離により、これらは既存画面を変えずに追加できる構成とする。
